Sometimes you need to override the title, description, episode number for Nova episodes.

This script crawls a folder, extracts title, season number, and episode number from filenames and generates a corresponding nfo file for each video so you can customize.

Use case: Nova episode files. 